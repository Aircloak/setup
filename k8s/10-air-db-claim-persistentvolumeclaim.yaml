apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    run: air-db-claim
  name: air-db-claim
  namespace: aircloak
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10G
